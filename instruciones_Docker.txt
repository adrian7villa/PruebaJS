1) Listar imagenes
    docker imagenes
2) Listar contenedores
    docker ps
    docker ps -a   Lista contenedores parados
3) Eliminar contenedores
    docker rm [id]

Enable-WindowsOptionalFeature -Online -FeatureName Microsoft-Hyper-V -All
DISM /Online /Enable-Feature /All /FeatureName:Microsoft-Hyper-V
docker run --name repo alpine/git clone https://github.com/docker/getting-started.git
cd getting-started
docker run -d -p 80:80 --name docker-tutorial docker101tutorial
docker run -it centos bash
docker run -d -p 80:80 docker/getting-sta
docker run -p 127.0.0.1:3306:3306  --name some-mariadb -e MARIADB_ROOT_PASSWORD=1234 -d mariadb:tag
docker run -d -p 4000:80 php:7.0-apache    
docker run -d -p 3000:80 php:5.5.38-apache



docker rm $(docker ps -aq)   //esto elimina todos los contenedores
docker ps -aq    //lista los id de los contenedores creados


docker build -t hellonode3 .  //comando para crear la imagen del docker basado en el dockfile

docker run -p 4000:3000 hellonode3  //comando para correr el contenedor y exponerlo por un puerto




  GNU nano 4.8                                                                                       Dockerfile                                                                                                 
  
FROM php:5.3-apache

COPY /usr/src/myapp /var/www/html

EXPOSE 80

